# Claude Code Guidelines

- Always update plugin versions in both `plugin.json` and `.claude-plugin/marketplace.json` when making changes to a skill.
- Always keep `README.md` in sync with any skill additions, removals, or description changes.
